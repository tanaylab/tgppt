% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppt-utils.R
\name{plot_list_ppt}
\alias{plot_list_ppt}
\title{Plot a list of ggplot objects to a PowerPoint file, one per slide}
\usage{
plot_list_ppt(
  plots,
  out_ppt,
  titles = NULL,
  template = NULL,
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{plots}{a list of ggplot objects}

\item{out_ppt}{output pptx file}

\item{titles}{optional character vector of slide titles, one per plot.
When not NULL, must have the same length as \code{plots}.}

\item{template}{path to a custom pptx template file. When NULL, uses
\code{getOption("tgppt.template")} if set, otherwise the bundled template.}

\item{overwrite}{overwrite existing file}

\item{...}{additional arguments passed to \code{\link{plot_gg_ppt}} (e.g.
\code{height}, \code{width}, \code{left}, \code{top},
\code{rasterize_plot}).}
}
\value{
invisible NULL
}
\description{
Creates a PowerPoint file with one slide per plot. Each plot is rendered
via \code{\link{plot_gg_ppt}}, so all of its options (rasterisation,
transparent background, etc.) are available via \code{...}.
}
\examples{
library(ggplot2)
plots <- list(
    ggplot(mtcars, aes(x = mpg, y = drat)) + geom_point(),
    ggplot(mtcars, aes(x = wt, y = qsec)) + geom_point(),
    ggplot(mtcars, aes(x = hp, y = mpg)) + geom_point()
)
temp_ppt <- tempfile(fileext = ".pptx")
plot_list_ppt(plots, temp_ppt)

# With per-slide titles
temp_ppt2 <- tempfile(fileext = ".pptx")
plot_list_ppt(plots, temp_ppt2, titles = c("Plot 1", "Plot 2", "Plot 3"))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppt-utils.R
\name{add_table_ppt}
\alias{add_table_ppt}
\title{Add a table to a PowerPoint slide}
\usage{
add_table_ppt(
  df,
  out_ppt,
  left = 2,
  top = 4,
  width = 25,
  height = 15,
  inches = FALSE,
  title = NULL,
  title_fp = NULL,
  template = NULL,
  new_slide = FALSE,
  overwrite = FALSE,
  font_size = 10,
  header_bold = TRUE,
  header_bg = "#4472C4",
  header_color = "white",
  alternating_rows = TRUE,
  even_row_bg = "#D9E2F3"
)
}
\arguments{
\item{df}{a data frame to render as a table}

\item{out_ppt}{output pptx file}

\item{left}{left position in cm (or inches if \code{inches = TRUE})}

\item{top}{top position in cm (or inches if \code{inches = TRUE})}

\item{width}{width of the table in cm (or inches if \code{inches = TRUE})}

\item{height}{height of the table in cm (or inches if \code{inches = TRUE})}

\item{inches}{logical; if TRUE, dimensions are in inches instead of cm}

\item{title}{optional slide title string. When not NULL, a title text box is
placed above the table.}

\item{title_fp}{an \code{fp_text} object controlling title font properties.
When NULL (the default), uses 18pt bold ArialMT.}

\item{template}{path to a custom pptx template file. When NULL, uses
\code{getOption("tgppt.template")} if set, otherwise the bundled template.}

\item{new_slide}{logical; add table to a new slide}

\item{overwrite}{logical; overwrite existing file}

\item{font_size}{font size for the table text}

\item{header_bold}{logical; if TRUE, header row text is bold}

\item{header_bg}{background colour for the header row}

\item{header_color}{text colour for the header row}

\item{alternating_rows}{logical; if TRUE, even rows get a coloured background}

\item{even_row_bg}{background colour for even rows when
\code{alternating_rows = TRUE}}
}
\value{
invisible; called for its side effect of writing a pptx file.
}
\description{
Creates a formatted table on a PowerPoint slide using the flextable package.
}
\examples{
temp_ppt <- tempfile(fileext = ".pptx")
add_table_ppt(head(mtcars), temp_ppt)
}
